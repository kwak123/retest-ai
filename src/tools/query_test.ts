import { TestStore } from "../store";

export async function listTests(store: TestStore) {
  // TODO: format list
  return [];
}

export async function getTestInfo(store: TestStore, args: { filePath: string }) {
  // TODO: get info
  return {};
}
